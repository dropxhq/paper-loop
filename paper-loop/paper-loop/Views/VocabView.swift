import SwiftUI
import SwiftData

struct VocabView: View {
    var body: some View {
        NavigationStack {
            Text("词表")
                .navigationTitle("词表")
        }
    }
}

#Preview {
    VocabView()
}

## 1. 词表字母索引导航

- [x] 1.1 在 `VocabView` 引入按首字母分组的数据结构（A-Z + #），并保证分组顺序稳定
- [x] 1.2 将词表渲染改为支持分组锚点的滚动列表，保留现有搜索与类型筛选能力
- [x] 1.3 实现右侧字母索引栏（按压/滑动），命中字母时滚动到对应分组首项
- [x] 1.4 实现“目标字母无词条”回退策略（跳到下一个可用分组）与交互提示

## 2. 原文阅读加载与失败状态

- [x] 2.1 设计并实现统一 Reader 状态容器（loading/success/failure）供 HTML 与 PDF 共用
- [x] 2.2 在 `SourceDetailView` 的“查看上下文”入口接入统一加载页与错误页
- [x] 2.3 在 `ReviewView` 的“回到原文”入口复用同一状态容器，保持交互一致
- [x] 2.4 为失败态提供重试操作与“改用 PDF 打开”恢复路径

## 3. 回链定位一致性修复

- [x] 3.1 梳理并统一 PDF 页码基准（导入 anchor 与 `PDFReaderView` 索引语义一致）
- [x] 3.2 校正 HTML/PDF 打开判定逻辑，避免 HTML 不可用时进入无效路径
- [x] 3.3 在 `HTMLReaderView` 补充导航失败回调与可见错误处理
- [x] 3.4 验证关键词高亮/定位逻辑在首段、中段、末段场景均可用

## 4. 验证与回归

- [ ] 4.1 手工验证字母索引滑动定位（含大小写、数字/符号、无匹配字母）
- [ ] 4.2 手工验证原文加载链路（弱网、HTML 404、PDF 回退、重试）
- [ ] 4.3 回归检查词表、复习页、回链详情页主要路径无行为退化
- [x] 4.4 更新必要的说明文档与变更备注（若涉及交互文案）
